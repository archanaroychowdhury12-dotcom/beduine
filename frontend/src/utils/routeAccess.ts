export {
  APP_ROUTES,
  buildLoginRedirect,
  getPostAuthView,
  getProtectedRouteRedirect,
  isProtectedView,
  normalizeNextPath,
} from './appRoutes';

export type { ProtectedRouteRedirect } from './appRoutes';
